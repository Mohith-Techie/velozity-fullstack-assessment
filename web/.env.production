# Production build (Vercel). Not secret. REST calls stay same-origin (web/vercel.json rewrites /api/* to
# Render), but Vercel can't proxy WebSockets, so the socket connects to the Render backend directly.
VITE_SOCKET_URL=https://velozity-fullstack-assessment.onrender.com
